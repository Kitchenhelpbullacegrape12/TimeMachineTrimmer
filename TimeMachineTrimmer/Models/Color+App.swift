import SwiftUI

extension Color {
    static let accentTeal = Color(red: 0x0F / 255, green: 0x6E / 255, blue: 0x56 / 255)
    static let actionCoral = Color(red: 0xF0 / 255, green: 0x99 / 255, blue: 0x7B / 255)

    static let timelineFresh = Color.white.opacity(0.75)
    static let timelineOld = Color.white.opacity(0.14)
    static let iconBackground = accentTeal
}
